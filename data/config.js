// Published Google Maps key for the live (GitHub Pages) map.
// Paste your key between the quotes below, commit, and the shared map works
// for everyone -- no one else needs a key.
//
// This is a BROWSER key: it is PUBLIC by design (any visitor's browser can see
// it). Do NOT rely on secrecy. Protect it in the Google Cloud Console:
//   1. HTTP referrer restriction -> your Pages URL only
//   2. API restriction -> Maps JavaScript API + Street View Static API
//   3. Daily quota caps + a billing budget alert (caps your cost hard)
//
// Leave it as "" to fall back to the in-browser key button.
window.GOOGLE_MAPS_KEY = "AIzaSyDSJ693bk2Gd_zEf4ZydEVvE6dScUaG0es";
